---
description: Install a durable Pi RPC service in the selected Sprite
---

Install or verify the Pi RPC host service in the selected Sprite, keep it private by default, create a local proxy, check `/health`, and provide concise example requests for `POST /rpc` and `GET /events`. Do not expose it publicly without configured bearer authentication.
